{
  "name": "team-dashboard",
  "version": "0.1.0",
  "description": "Comprehensive team management dashboard for Claude Code instances",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --recursive --filter=!@team-dashboard/docs --filter=!@team-dashboard/dashboard build",
    "start": "pnpm run --parallel start",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:performance": "vitest run --config vitest.performance.config.ts",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:coverage:watch": "vitest watch --coverage",
    "coverage:report": "vitest run --coverage --reporter=html",
    "coverage:json": "vitest run --coverage --reporter=json",
    "coverage:lcov": "vitest run --coverage --reporter=lcov",
    "coverage:cobertura": "vitest run --coverage --reporter=cobertura",
    "coverage:check": "vitest run --coverage --reporter=text-summary",
    "coverage:badge": "node scripts/generate-coverage-badge.js",
    "coverage:summary": "node scripts/coverage-summary.js",
    "validate": "pnpm run typecheck && pnpm run lint && pnpm run format:check && pnpm run test:run",
    "coverage:full": "pnpm run test:coverage && pnpm run coverage:badge",
    "test:smoke": "node test/smoke/smoke-tests.js",
    "prepare": "husky install",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.7.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@vitejs/plugin-react": "^5.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "eslint": "^8.56.0",
    "eslint-config-next": "^14.1.0",
    "happy-dom": "^18.0.1",
    "husky": "^8.0.3",
    "jsdom": "^26.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.1",
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@gannonh/memento-mcp": "^0.3.9",
    "@modelcontextprotocol/sdk": "^1.17.3"
  }
}