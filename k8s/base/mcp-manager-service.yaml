apiVersion: v1
kind: Service
metadata:
  name: mcp-manager
  labels:
    app: mcp-manager
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: mcp-manager