apiVersion: v1
kind: Service
metadata:
  name: agent-manager
  labels:
    app: agent-manager
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: agent-manager