apiVersion: v1
kind: Service
metadata:
  name: openai-service
  labels:
    app: openai-service
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3003
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: openai-service